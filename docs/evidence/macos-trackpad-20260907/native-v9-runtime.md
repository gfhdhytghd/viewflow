# Native bridge runtime, version 9

Normal restart: 2026-09-08 16:36:56 EDT. PID 311 runs the installed version 9
DEXT at SystemExtensions/01B670A3-0AA3-462F-AEC2-8255E4657973. Installation
signature/profile/hash evidence is in native-v9-install-result.md.

The native chain is now VFTrackpad → IOHIDInterface →
AppleMultitouchTrackpadHIDEventDriver → AppleMultitouchDevice →
AppleMultitouchDeviceUserClient (WindowServer PID 172). The child exposes
parser 1000/options 39, width 16000/height 11490, rows 22/columns 30 and
zero Critical Errors. See native-v9-runtime-tree.json.

At 16:37:02 the public CPU-copy path successfully returns report 0, query
length report 1, the 74-byte 0xdb composite, and 0xc8. Mode report 2 is
accepted. All provided report buffers are now accessed successfully.

Five unsupported writes remain: setReport 0xc8, length 2, at 16:37:02.472.
Status reports ABI 2/profile 1, native attachment true, five gets/eight sets,
five unknown features, zero submitted input reports and zero input errors.

Version 10 implements that configuration byte as mutable state: initialize
to 8, accept correctly framed two-byte writes, and return the stored byte on
subsequent reads. Unknown report IDs still fail. Offline tests verify initial
value, write/readback and malformed-write preservation. No input report is
generated by feature initialization or tests.

Protocol comparison: the [VoodooInput simulator](https://github.com/acidanthera/VoodooInput/blob/master/VoodooInput/VoodooInputSimulator/VoodooInputSimulatorDevice.cpp)
returns 0xc8/8 and acknowledges writes. Viewflow implements its own bounded
stateful register behavior; it does not import that kernel extension.

This verifies native initialization progress; physical gestures are still
pending user-operated acceptance.
