# Viewflow 交接：跨系统窗口拖动（2026-09-06）

> 2026-09-07 用户新增全项目指导原则：安全限制不得比 Windows remote window 和 Sunshine/Moonlight 更严格；33ms只是性能目标，不能作为退出条件。以 [AGENTS.md](AGENTS.md) 和[新原则](docs/security-and-availability-policy.md)为准，覆盖本历史快照的严格时限要求。

> 后续进度已移到 [docs/project-status.md](docs/project-status.md)。下文保留为离职交接快照；其中“尚未重新编译”等状态不能覆盖后续验证记录。

## 接手前必读

用户最新要求：**“写一份交接文档，我要让另一个 agent 接手了，这个账号还有 1% limit。”** 因此本轮停止继续扩展实现，不再做长测试。此前具体任务是：**“尝试直接把跨系统窗口拖动实现了，然后告诉我怎么启动，我来一次全测试了。”** 更早的总目标是完成项目 plan，不能把本次局部工作当作整个 plan 已验收。

**当前不是全部完成/可保证直接跑通的状态。** 主干链路和启动包已实现、较早版本通过编译测试；最后追加的动态窗口发现与异步捕获加入仍是 WIP，刚修复语法错误，尚未重新编译。先完成下文 P0，再向用户交付试跑。不要重复宣称全功能/物理验收完成。

工作目录 `/home/wilf/data/viewflow`。Git `main` **没有任何提交**，几乎整个项目都是 untracked；不能靠普通 git diff 判断本轮变化，更不能清理/reset 用户工作。未替用户 commit。不要删除历史 release/target/source 目录。

本次按 Linux/Hyprland 应用窗口 → Windows 原生代理窗口 → 拖回同一个 Linux 窗口实现。应用进程始终在 Linux。**不是 Windows 原生应用捕获，也不是进程迁移**。此范围已写入 `docs/desktop-drag-contract.md`，不能偷换成整个双向平台目标完成。

用户要自己全测；本轮没有在主桌面加载 Viewflow 插件、创建输出、移动窗口、启动收发会话或发模拟输入。仅编译、单元/假 IPC 测试、读取环境、复制测试包。不要擅自启动 UI 全测。

## P0：接手者先做什么

1. 读 `crates/viewflowd/src/desktop_source.rs` 和 `atlas_source.rs` 最新动态发现代码，然后执行：

   ```sh
   cd /home/wilf/data/viewflow
   cargo check -p viewflowd --bin vf-media-peer --features native-gpu-nvenc
   cargo test -p viewflowd --lib --features native-gpu-nvenc desktop
   cargo test -p viewflowd --lib --features native-gpu-nvenc
   cargo fmt --check
   cargo clippy -p viewflowd --lib --features native-gpu-nvenc -- -Dclippy::all
   ```

   最后一次 check 在 agent 修改中读到 `lane_from_config` 尾部返回错误、cleanup 三元 match 缺 `(Ok,Err,Err)`；source_recovery 已通知这两处及 shutdown match 分支类型修复完成，**未重跑 check**。不要把之前 482 个通过测试套到当前 WIP 上。

2. 重点审阅动态加入资源生命周期（下节）。之前“auto_enroll”其实只遍历静态候选，启动脚本只提供第一个窗口，导致第二个窗口永远不加入；这是最后追加 WIP 的原因。必须保留真正的运行时本地发现，而非退回单窗口冒充完成。
3. 重编 Linux Release；当前 `target/release/vf-media-peer` **落后于最新 source discovery、probe crop 修正和 probe 信号清理修改**：

   ```sh
   cargo build --release --locked -p viewflowd --bin vf-media-peer --features native-gpu-nvenc
   cmake --build build/desktop/capture --parallel 2
   cmake --build build/desktop/input --parallel 2
   tools/tests/desktop-drag-launchers-test.sh
   ```

4. 如果修改了 Windows 共用 Rust/协议/C++，按后文重编并更新 Windows 包。不要拿旧二进制冒充最新源码。最后才把启动方式交给用户，明确尚待用户真实桌面验收。

## 动态发现 WIP 的具体状态和审阅点

owner 原子任务是 source_recovery，已停止扩展并交接。主要文件：`desktop_source.rs`、`atlas_source.rs`，附带 GPU capture/session/pool 和 input policy。

当前设计：

- `DesktopEnrollmentSupervisor` 每 100ms 把本地 `j/clients` 查询放进 `spawn_blocking`，socket timeout 50ms；仅 mapped/visible、与 remote viewport 相交的本地窗口候选进入队列。
- 最多 8 个已加入窗口，候选/工作队列有界；同一会话、同一 atlas，不给每个窗口另开独立完整会话。
- 必须匹配本地 stableId、PID、address，并用真实 HCGI 的 address/PID/surface 复验。初始窗口也必须保留启动 probe 的 PID/stableId，不再使用 `expected_pid=None`。
- metadata probe **没有 GPU 读取**，所以不能调用 `release_after_source_reads` 冒充完成。最后已改成：保留首个 GpuFrame → 显式 stop probe stream → 丢弃 frame → 为实际 atlas 启动一个新 stream。实际新帧仍须由 atlas 正常导入/验证，旧 metadata 只作 preflight。
- query/probe/cleanup handles 必须全部由 supervisor 持有，在 `supervisor.shutdown()` 中逐一收集；不允许 `tokio::spawn` 后丢 handle，不允许首个 cleanup 错误提前 return 而跳过其他 producer。
- `atlas_source::run_live` 结束顺序是 supervisor shutdown → input shutdown → capture session shutdown；三个 cleanup 即使有错误也都执行。
- `poll_one` 在本地候选不满足/关闭时丢弃候选，不应让无关窗口关闭终止现有媒体。`add_and_reserve_if_eligible` 出错时仍要停止手上的 stream。
- 最后 agent 提醒要审阅 `probe_candidate` 的 async closure、JoinSet shutdown、initial lane preflight 与 `desktop.candidates` 对齐。

先前 native-gpu Clippy 有两个 hard errors：`run_live` 8 个参数；`gpu_compatible_encoder::AtlasSubmitOutcome` large_enum_variant（追加 desktop 后最大 variant 248 bytes）。agent 已收到修复要求，需确认当前是否修好。其余约 37 个 pedantic/dead-code warnings 不等于本轮全新增，也未承诺零 warning。

## 已实现的链路

### 协议与帧布局

- `crates/viewflow-protocol/src/desktop.rs`：`DesktopRect`（milli-DIP）、`AtlasDesktopLayout`、每窗口 placement，`DesktopWindowMove` / Ack。control tags 43/44；AtlasFrame optional desktop field 13。
- desktop layout 必须与 atlas tiles 严格一对一，允许窗口部分/完全在 viewport 外；由 Windows 裁剪/隐藏，不得把“超出 viewport”当无效协议。
- V7 native record：旧 V5 112 字节头 + N×64 tile，后接 48 字节 desktop header + N×56 placements；总头 **160+120N**。无 desktop 仍走旧 V5，V6 是恢复控制，不混为图片。
- `gpu_presenter_pipe.rs` / `atlas_presenter*.rs` / `atlas_session.rs` / `atlas_receiver_presenter.rs` / `atlas_peer.rs` 已接好 V7、独立 move queue、input recovery fence 和 Ack 路由。

### Windows 原生呈现/拖动

- `platform/windows-composition-preview/main.cpp`、`desktop_layout.h`、`desktop_move_gesture.h`、parser 等。
- Windows 手势是 **按住 F8 + 鼠标左拖**；F8 保留，不发给 Linux 应用。没有叠加第二个标题栏。
- C++ 只根据后续 V7 捕获帧改变 HWND/纹理位置，不会收到 move 请求就本地乐观移动。
- 全窗口逻辑边界 → 显式物理 viewport 裁剪，窗口内指针逆映射回完整 tile；不要把 atlas 编码坐标当桌面位置。
- native argv 尾部：`--atlas-desktop-v1 --atlas-desktop-global-origin <xmilli> <ymilli> --atlas-desktop-physical-rect <x> <y> <w> <h> --atlas-desktop-scale-millidip-per-pixel <scale>`；严格 ready 追加 `desktop=v1 move_mode=f8`。
- 已修复编造 Win32 API：必须是 `SetCapture` / `GetCapture` / `ReleaseCapture`，不是不存在的 SetPointerCapture/ReleasePointerCapture。
- 正常 move deadline 源自 OS pointer timestamp、33.333334ms；Cancel 的清理控制也已在 main.cpp stamp 成相同有界预算，而非 helper 默认 250ms。

### 输入安全与 source 原生移窗

- `desktop_pointer.rs` 解析严格 22-field stdout move；`atlas_pointer.rs` 给 pointer/keyboard 和 desktop 两个通道记录统一单调 **ingress ordinal**，避免分队列丢跨通道顺序。
- `desktop_receiver_state.rs` 独立 Begin/Update/End/Cancel 状态、逐个 Ack、原始期限；Ended Ack 后，旧 ordinal 的应用事件不能重放。
- `atlas_preview_input.rs` Begin 前等待应用 FIFO/ACK idle，清除旧指针/键盘授权；拖动中不转发 app input；End 后必须下一次正常本地 selection 得到新 source grant。
- `window_input_runtime.rs` / `window_input_switch.rs`：source 真正发送原生 END，等 `Outcome::Ended` → `DesktopPaused`，**之后**才将 move 投递给 source controller；不是仅写了未调用的 helper。
- resume 已修复：同窗口 no-op drag 允许 epoch 不变，但 frame/generation 必须更新；切换另一个窗口不能拿独立 epoch/frame 与旧窗口比较，必须新 binding、新 generation、相同 owner/device。
- `atlas_source_input.rs` 的 DesktopMoveController 查 **32 帧 retained history 中 base_atlas_frame**，不能用最新一帧；base_geometry_epoch 比较 tile epoch，不能比较 atlas epoch。
- native enrollment token 独立有效 30s；每条 move 仍只有原始事件期限，不能把 token expiry 设成第一条事件的 33ms。End 先应用最后坐标再 release；Cancel restore；清理只处理自身 token。
- `crates/viewflow-hyprland/src/desktop_window.rs` 与 `platform/hyprland-plugin/src/desktop_window_controller.*`：私有 request file + `hl.plugin.viewflow.desktop_window(path)`，精确 window/PID/surface，opaque token 和严格 sequence，Hyprland ConfigActions 移动全捕获边界。
- 已用真实只读调用核实：Hyprland 0.56.2 `hyprctl eval 'return true'` 返回 `ok`，`repl` 返回 `true`。客户端现在发 raw `eval hl.plugin.viewflow.desktop_window(...)`，检查 `ok` 后读权威 JSON reply。
- **两个 socket 不同**：desktop.hyprland_socket 是 compositor `.socket.sock`；pointer.native_socket 是 Viewflow 自己的 seqpacket listener。真实 compositor socket 为 0755，父目录 0700。校验允许 owned、非 group/world writable socket + owned 0700 parent，仍验证 SO_PEERCRED PID/UID。

## 启动工具与已准备的试跑配置

通用工具：`tools/desktop-drag-pair.sh`、`tools/desktop-drag-linux.sh`、`tools/desktop-drag-windows.ps1`；说明 `docs/desktop-start.md`。

- prepare 读当前 monitor/instance，生成新 TLS/IDs 和配对 JSON；atlas 编码尺寸独立于 viewport，默认 4096²、decoded budget 256MiB。
- Linux start：仅创建自己的新 headless 输出 → 加载 capture plugin → 等首个 crossing（或 --window）→ probe 并生成 runtime source JSON → validate-send → 加载 input/native-control plugin → sender。
- **只有两个插件**：viewflow-capture.so 和 viewflow-hyprland.so；不存在第三个 native-control.so。不会替换/卸载用户已有 HyprCapture。
- Stop 只停记录 PID（校验 start time），移除自己的 output、卸载自己加载的插件；保留日志/有效配置到 `.stopped.*`，同一路径可再次启动。不要删故障证据。
- Windows 用 DPI-aware monitor inventory 检查配置的 physical viewport 被实际屏幕包含，先 validate-receive，再 receive；不是 SSH 假桌面的分辨率探测。
- CLI 新增 `probe --compositor-pid PID --window ADDRESS`、`validate-send --config JSON`、`validate-receive --config JSON`。source template 初始 windows=[]，只能在 probe seed 后 validate-send。
- 最后 root 修改 probe：输出 **crop_width/crop_height** 而非 allocation image_width/height；probe SIGINT/SIGTERM 等待有界 probe 完成 stop，再返回取消。这些修改还没进入最终 Linux Release。

当前已生成私有配置：`/home/wilf/.local/state/viewflow/desktop-test-20260906/`。含 send.json、receive.json、新 CA/双端证书。目录0700、key0600，七天有效。**不要输出密钥内容、不要复制 source.key/CA key 给 Windows、不要提交这些文件。**

当前配置：Linux 172.16.105.62:44130 → WindowsVM 172.16.105.70:44129。Windows viewport 是**刻意选取**的左上角 (0,0) 1024×768、1000 milliDIP/px；Linux remote viewport (3072,0) 1024×768。不是已探测 Windows 真实显示器分辨率。atlas 4096×4096，与显示位置分离。

准备了快捷脚本（先完成 P0 和重编才交付）：

```sh
bash /home/wilf/data/viewflow/build/desktop/start-linux.sh
# 等 waiting crossing 后，把一个浮动 Linux 窗口拖过 DP-4 右边界
bash /home/wilf/data/viewflow/build/desktop/stop-linux.sh
```

Windows 用户桌面双击：`C:\Users\wilf\Viewflow\desktop-test\start-windows.cmd`，应先启动接收端，再启动 Linux。Windows F8+左拖移动 Linux 窗口，向左返回。先试较小浮动窗口。用户自己完成真实跨屏/多窗口/鼠标键盘/退出全测。

## 环境与构建位置

最近只读确认：主 Hyprland PID **3386992**、0.56.2 commit efb509…；DP-4 6144×3456、scale2、transform0，逻辑3072×1728@(0,0)，60Hz。只有 DP-4，无本轮创建的 headless。主配置没改。已加载用户插件含 HyprCapture，不能卸载。

compositor command socket 来自 `$XDG_RUNTIME_DIR/hypr/$HYPRLAND_INSTANCE_SIGNATURE/.socket.sock`。native input 默认 `$XDG_RUNTIME_DIR/viewflow/hyprland.sock`；不要事后通过环境变量重定向已运行 compositor 的插件。

Linux构建目录：

- `build/desktop/capture/viewflow-capture.so`
- `build/desktop/input/viewflow-hyprland.so`
- `target/release/vf-media-peer`

Windows SSH：`wilf@172.16.105.70`，主机名 WindowsVM。只读 SSH Forms 返回的 WinDisc 1024×768 是非交互桌面，**不是物理验收证据**。私有/Public firewall profile 当前 disabled；本轮没改防火墙。

可重复使用的**已知由本任务持有**编译 staging：
`C:/Users/wilf/AppData/Local/Temp/viewflow-input-20260906-J4vBWv`

它含 Cargo 工作区和 `native-build`，可增量同步源码：

```sh
tar -cf - Cargo.toml Cargo.lock crates protocol platform/windows-composition-preview |
  ssh wilf@172.16.105.70 'tar -xf - -C C:/Users/wilf/AppData/Local/Temp/viewflow-input-20260906-J4vBWv'
ssh wilf@172.16.105.70 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File C:/Users/wilf/AppData/Local/Temp/viewflow-input-20260906-J4vBWv/build-release.ps1'
ssh wilf@172.16.105.70 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File C:/Users/wilf/AppData/Local/Temp/viewflow-input-20260906-J4vBWv/build-phase.ps1'
```

build-release.ps1：offline/locked Release vf-media-peer。build-phase.ps1：MSVC CMake Release + ctest（25 tests）。CMake路径：`C:/Program Files (x86)/Microsoft Visual Studio/2022/BuildTools/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe`。

**Windows 头文件 ABI 变化后要 clean-first**。本轮 Parser/AtlasLayout 加字段，旧 test objects 与新静态库混用，6 个老 parser tests SEGFAULT；干净重编后25/25通过，不能当作仍有 parser bug，也不能忽略未来真正失败。

已创建最终测试目录 `C:\Users\wilf\Viewflow\desktop-test`，ACL仅 wilf / SYSTEM / Administrators。已复制两 exe、receive.json、receiver.pem/key、pair-ca.pem、Windows launcher、start-windows.cmd；**没启动**。该目录现在存在，`build/desktop/provision-windows.ps1` 会拒绝覆盖，不能盲目重跑它。后续更新前检查自身文件/进程，按精确文件复制。

当前 Windows 包 SHA256（后续重编会改变）：

- vf-media-peer.exe `0D7B8846DE2E1D207E74E0393292B2AB16D2527039B57AF68939D533621C82C4`
- viewflow_windows_composition_preview.exe `418103C6A5E13F1D539889E348B6972AC5B6A301F7963D27CDA5FA14358CB3C3`

这版 Windows 包包含 ingress ordinal、desktop ACK、V7、Cancel deadline 修复和 validate CLI。其后主要修改在 Linux cfg-only 模块；仍应按实际源码判断是否需要重编，不要只看这句话。

## 已有测试证据（注意时间边界）

**在最后动态发现 WIP 之前**：

- `cargo test -p viewflowd --lib --features native-gpu-nvenc`：482 passed，8 ignored，0 failed（490 tests）。ignored 包含需要显式设备/窗口的真实测试，不能称物理验收通过。
- desktop focused：20 passed，包括 native END pause、no-op/切窗 resume、ordinal、V7/ready、source retained frame/tile epoch、30s token。
- Windows MSVC 两 exe Release build 成功；native ctest 25/25。
- Linux capture CTest 6/6，input plugin CTest 8/8。
- viewflow-hyprland desktop adapter 3 tests + strict Clippy。
- Launcher hermetic test通过（假 Hyprland/peer），含同 state path 重启、crossing、seed、validate、私钥权限。
- 生成 source template 用仅供离线校验的假窗口副本通过 validate-send，副本已移除；Windows实际 receive.json 通过 validate-receive。

**当前最后动态发现 WIP 尚未重新 check/test/build**。不要把历史成功覆盖到当前状态。

## 其他约束和上下文入口

优先读 `docs/desktop-drag-contract.md`、`docs/desktop-start.md`。更早全项目目标/验收上下文在 README、`docs/integration-acceptance-gates.md`、`docs/window-input-integration-status.md`，本文件不声称替代全部 plan。

Hyprland Lua 操作需先读 `/home/wilf/.codex/skills/hyprland-lua/SKILL.md` 及其必要引用；使用当前 Lua provider，不能对 Lua 配置套旧 `hyprctl keyword`。临时输出脚本已用 `hl.monitor({output,mode,position,scale})`。

当前相关 agent 都已完成或停止；用户要求交接是优先事项，不要让旧 agent 继续后台修改。接手时先重新检查文件和编译状态，不依赖对话里的“完成”字样。不要读取/运行 staging 里的旧 SendInput、IME、焦点测试脚本；那些属于以前的验收工作，不在这次用户自测授权范围。


## 2026-09-06 Windows 优先与显示器配置追加交接（最新）

当前优先级为跑通 Linux → Windows，macOS 实机按用户要求暂缓。显示布局已改成 Hyprland 风格的物理分辨率、常规缩放、全局逻辑坐标，支持负坐标/错位；不再使用边缘百分比连接。最新权威状态见 `docs/project-status.md`、`docs/display-layout.md`，实机成功与失败及当前二进制哈希见 `docs/evidence/desktop-coordinate-20260906.md`，不要继续使用本文件上文的旧哈希/旧配对路径。

本轮最后运行 29：完整 Windows 鼠标/按钮/Shift+A/双轴滚轮输入后，副屏 1536×1152、scale 1.5、位置 (-1024,40) 连续十次往返通过；同一个 HWND 6489902，本地最终位置 (2,72)、尺寸 791×598，源端计划退出 0。2181 次原生视觉提交不等于物理逐帧呈现证明。之前仍出现原生或选择超时，长期稳定性与剩余产品功能尚未验收。

桌面控制器现在由独立 worker 持有，读取已提交不可变帧历史；不会因 GPU owner 等待或媒体完成取消在途原生命令。应用输入通过阶段栅栏和 routed pause 保护。Update 的失败也记账原生已尝试序号，清理只在明确 sequence rejection 时尝试前一序号，期限/token/身份不变。原生功能 Rust 测试 493 通过、8 忽略；新增后台调度和不确定序号清理回归通过。

Windows 手工试用目录 `C:\Users\wilf\Viewflow\desktop-test` 已更新并校验，旧八个文件备份在 `backup-20260906-210101`。Linux `build/desktop/start-linux.sh` 指向 `/home/wilf/.local/state/viewflow/desktop-test-20260906-coordinates/send.json`，与 Windows 是同一新配对。试用布局远端物理 1536×1152、scale 1.5、逻辑 (3072,0)，独立 atlas 2048×1536，Windows 本地 (0,0)。未自动启动主桌面会话；所有自建嵌套测试与 Windows 临时任务结束清理。


### 主桌面启动与相邻布局补充

用户要求直接在主系统启动，并将副机同时放在 DP-4 右侧、HEADLESS-1 上侧。实测 DP-4 逻辑 (0,0) 3072×1728，HEADLESS-1 为 (3072,1590)，所以副机物理 1536×1152、scale 1.5 的逻辑 1024×768 设置为 (3072,822)。两端当前配对 JSON 已同步，原 JSON 有 before-adjoining 备份。

主桌面暴露后台工作区旧坐标被误当跨屏窗口的问题；启动器与动态发现现均限定当前显示的工作区，新增动态发现回归通过。插件加载会触发主配置重载，启动器在每次加载后重新应用副屏布局。新显示器不覆盖用户已有 HEADLESS-1。

主桌面短时建立过 Linux/Windows 连接，但本地鼠标触发 LocalMotion 撤销后会终止会话，随后静态窗口直接恢复出现 drained atlas startup timeout。没有宣布主桌面长时稳定。最后移除自建测试窗口，Linux 启动器在等待用户浮动窗口跨入副屏，Windows 交互任务 ViewflowMain-Active 负责接收端；相关启动日志在 /home/wilf/.local/state/viewflow/main-session-20260906-212600/waiting-launcher.log。

### Dolphin 试用与编码超时恢复追加

最新状态见 `docs/project-status.md` 主桌面补充。启动过早接管输入已改为等待 Windows 明确选择；初始时钟探测等待原生就绪，动态接纳限定可见浮动窗口并预检 atlas 容量。Rust 原生测试 496 通过、8 忽略，launcher hermetic 通过。一次主桌面会话 Windows 提交 2330 帧后遇到编码超时；此时原生输入 authorized=0，未因本地鼠标取消。

NVENC 新增独立 status 9 `EXPIRED_AFTER_SUBMISSION`：只有匹配 PTS 的包已排出、GPU 读取及导入清理成功才可恢复；丢弃包，native 强制下一次 IDR，Rust 清除 alpha 缓存并请求 IDR，消费旧源序号。status 8 的无 NVENC 提交含义不变，legacy 入口仍终止。原生 GPU 故障注入（stage 5）、C ABI 恢复及强制 IDR 检查随六项 CTest 通过。测试日志 `/tmp/viewflow-postencode-native-tests.log`、`/tmp/viewflow-postencode-tests.log`。仍可能 Windows 首帧在视觉变更后超时退出；不能宣布会话稳定。

鼠标 Linux → Windows 跨屏尚未实现。插件已有 InputCapture 和标准 InputLease/InputEvent，但 atlas 只接入反方向应用输入；需要在同一个认证连接集成，按全局逻辑矩形判定边界，解决 owned 虚拟输出被当本地邻居及捕获期间回传窗口输入冲突。不得以另开 Deskflow 会话、百分比配置或单纯隐藏/移动指针冒充完成。

### 2026-09-07 主桌面输入追踪（覆盖前文旧尺寸与跨屏状态）

用户明确 Windows 为 3840×2400、2 倍缩放；owned HEADLESS-6 现为该尺寸/scale、逻辑 (3072,390)，atlas 2048×2048。用户授权分子 agent 处理 Win 拖动、blur、容量恢复、Linux 鼠标跨屏；对应代码已合入。Linux 跨屏通过既有认证连接已完成一次真实往返，后续持续输入仍有失败，不能宣布产品稳定。最新细节、测试与二进制哈希见 `docs/evidence/desktop-main-input-20260906.md`。

已修复合法时钟误差使满预算 InputEvent 被接收端拒绝（发送端收紧 5.5ms）、第一次 selection/授权互等造成64项队列填满、部分原生呈现阶段诊断丢失、以及 disposition 模式重复 admission deadline 检查。接收端期限和原硬件事件时间仍保持。最近单测试窗口达到 2502 次视觉提交并收到初始授权，但点击引用的捕获帧年龄约41.9ms超过33.3ms，等待 selection 过期使会话退出；未执行键盘或 Win 拖动实机检查。

工作中的独立测试窗口是 root 自建 Qt `Viewflow input check`，用于按钮/键盘日志，不是用户 Dolphin。临时 fixture-only source 配置的原配对配置备份为 `/run/user/1000/viewflow/desktop-drag/config-before-fixture-only`，完成测试后必须恢复。当前会话因上述错误停止，不要写“正在运行”。后续正在实现选择拒绝后的严格撤销/恢复以及关闭窗口独立移除；最终状态必须重新核验，不将这些 WIP 当已完成。


## 2026-09-07 手动验收交接

用户要求实机测试本人操作；禁止继续自动移动鼠标、自动按键或自动桌面输入测试。代码检查、构建和部署已经完成，本版身份/限制见 `docs/evidence/desktop-main-input-20260906.md` 最后一节。原 Dolphin + auto_enroll 配置已恢复，自动测试Qt窗口PID2844106已清理；主会话已重新启动，PID文件在 `/run/user/1000/viewflow/desktop-drag/daemon.pid`，须核对starttime后才管理进程。勿运行仍含旧fixture PID的 `/tmp/viewflow-fixture-input-recovery-*.py`。

输入法按应用源系统归属，只转发物理按键。新增原生WaylandIME/候选框捕获、窗口内候选命中、Windows完整按键FIFO和Win拖动取消尾事件处理，尚未实机手动验收。窗口外候选可显示但不可点击；其他候选仍可能因atlas容量未加入；仍有干净过期帧。不要宣称完整产品完成。


### 2026-09-07 跨屏反馈后最新交接

已部署双端精确selection成功收据修复，主源进程启动时PID3960156，操作前核对state中的PID/start。owned显示器已恢复3840×2400、scale2、(3072,390)、ID2。启动存活不是跨屏验收；仍由用户手动测试，禁止自动输入。详见docs/evidence/desktop-main-input-20260906.md末尾。使用修正后的/tmp/viewflow-deploy-ime-current.py在所有插件加载后恢复布局；/tmp/viewflow-resume-main.py启动前读回核验。旧fixture已关闭，Dolphin/auto-enroll配置恢复。


最新合并版本：悬停激活分离、native END62保留焦点清理、cursor ACK失败确认rollback保留media。两端与Linux插件必须同步更新。最新已部署源PID55909，操作前核验state/start，实机仅用户手动，禁止自动输入。最新证据和哈希见docs/evidence/desktop-main-input-20260906.md末尾。
